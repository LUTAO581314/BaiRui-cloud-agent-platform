# 领域术语

## 智能体

当前用户创建和管理的云 Agent 项目。智能体名称是当前用户范围内不可重复的业务唯一标识，用于界面检索、跳转和配置关联；后端可维护不在界面展示的内部主键。

## Agent 会话

由一个智能体处理的用户对话记录。会话管理首页按智能体聚合，单个会话属于唯一的智能体。

## Agent 发布

将一个用户拥有的智能体配置版本交付为可运行版本的记录。普通用户只能查看本人智能体的发布状态与历史；服务器级命令、备份、回滚和控制平面操作不属于当前用户控制台。

## GitHub 仓库绑定

当前 MVP 中，智能体的代码来源固定为 GitHub 仓库及其默认部署分支。GitHub 授权、Webhook 与凭据属于服务端安全边界，前端只展示脱敏后的绑定状态和仓库元数据。

## Agent 对话服务入口

用户访问某个智能体前端服务的地址。当前 MVP 从智能体卡片的“对话”入口以新标签页打开对应地址；服务尚未上线时，仅展示无数据、无输入框的占位提示页。
